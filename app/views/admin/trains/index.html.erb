<p id="notice"><%= notice %></p>
<div class="page-header">
<h1>Список поездов</h1>
</div>

<table class="table table-striped">
  <thead>
  <tr>
      <th>Номер</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @trains.each do |train| %>
      <%= render train %>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Создать новый поезд', new_admin_train_path, class: 'btn btn-primary', role: 'button'  %>


<br>
<%= link_to 'Назад', 'welcome/index', class: 'btn btn-primary', role: 'button'  %>
