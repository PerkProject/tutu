<p id="notice"><%= notice %></p>

<div class="page-header">
  <h1><%= t('.title') %> <small></small></h1>
</div>

<table class="table table-striped">
  <thead>
  <tr>
    <th><%= t('.name') %></th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @railway_stations.each do |railway_station| %>
      <%= render railway_station %>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to t('.add'), new_admin_railway_station_path, class: 'btn btn-primary', role: 'button' %>

<br>
<%= link_to t('.back'), 'welcome/index', class: 'btn btn-primary', role: 'button' %>
